public class LifeApp {
    public static void main(String[] args) {
        /*
        The application instantiates a model.
        It then instantiates a view, passing it a reference to the model.
        Finally, it tells the view to run()
         */
    }
}
